name: ikhaya_health
description: Offline-first healthcare records system for rural clinics
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  provider: ^6.1.0
  http: ^1.1.0
  shared_preferences: ^2.2.0
  encrypt: ^5.0.3
  permission_handler: ^11.0.0
  
  # Database - UPDATED FOR DESKTOP SUPPORT
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0  # NEW - For desktop platforms
  path_provider: ^2.1.0
  path: ^1.8.3
  uuid: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/